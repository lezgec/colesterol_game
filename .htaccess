Options -Indexes

<FilesMatch "^\.env($|\.)">
    Require all denied
</FilesMatch>

<FilesMatch "^(composer\.(json|lock)|package(-lock)?\.json|schema\.sql|seed\.sql)$">
    Require all denied
</FilesMatch>

RedirectMatch 403 ^/(.+/)?(app|config|database|frontend|includes|vendor)(/|$)
