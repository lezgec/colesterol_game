# Frontend

Esta carpeta contiene piezas de presentación reutilizables que no son endpoints.

- `components/`: componentes PHP compartidos por las vistas, como iconos inline y menú de usuario.

Las rutas públicas actuales siguen en `pages/` y los recursos estáticos en `assets/` para mantener compatibilidad con el despliegue actual en XAMPP/Apache.
