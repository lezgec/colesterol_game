# Pruebas funcionales - 2026-06-29

Entorno: XAMPP local en `http://localhost/colesterol_game/`.

## Resultado

Estado general: aprobado.

## Checklist ejecutado

- Login superadmin: aprobado.
- Logout: aprobado.
- Registro de jugador temporal: aprobado.
- Jugador intentando acceder a admin: aprobado, acceso rechazado.
- Login docente temporal: aprobado.
- Docente intentando iniciar sala ajena: aprobado, acceso rechazado.
- Pregunta maliciosa con `<script>`: creada temporalmente para validar flujo y limpieza.
- Crear sala temporal: aprobado.
- Agregar pregunta a sala por endpoint: aprobado.
- Iniciar sala con una pregunta configurada: aprobado.
- Unirse a sala: aprobado.
- Intentar unirse dos veces con el mismo nombre: aprobado, devuelve `duplicate_player_name`.
- Responder pregunta en modo solo: aprobado.
- Exportar CSV global: aprobado.
- Exportar PDF global: aprobado, archivo generado con cabecera `%PDF-`.
- Recuperacion de contrasena con correo no registrado: aprobado, respuesta generica.
- Gemini rate limit: verificado en codigo sin consumir API externa.

## Validaciones tecnicas

- Lint PHP fuera de `vendor`: aprobado.
- `node --check` para JS en `assets`: aprobado.
- Limpieza de datos temporales `CODEX_TEST_*`, `CODEX_XSS_*`, `codex_player@example.invalid` y `codex_teacher@example.invalid`: aprobada.

## Nota

No se ejecuto una llamada real a Gemini para evitar consumo externo durante la prueba automatizada local. Se verifico que `generate_questions_bulk.php` aplica `require_rate_limit` al flujo Gemini.
